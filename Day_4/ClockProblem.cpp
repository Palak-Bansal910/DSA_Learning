#include<iostream>
using namespace std;

int findAngle(int hour, int minute){
    if(hour < 0 || hour > 12 || minute < 0 || minute > 60){
        cout << "Invalid input." << endl;
        return -1;
    }
    
}
int main(){

    return 0;
}